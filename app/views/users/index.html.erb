<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>

<table class="table">
  <thead>
    <tr>
      <th>Role</th>
      <th>User name</th>
      <th>Email</th>
      <th colspan="3">action</th>
    </tr>
  </thead>
  <tbody>
      <%= render partial: 'listitem', collection: @users, as: :user %>
  </tbody>

</table>
<%= will_paginate @users %>
<br>

<%= link_to 'New User', new_user_path %>
